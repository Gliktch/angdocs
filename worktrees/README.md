# Worktrees

Ignored local patch worktrees and per-batch branches.

Use this directory for focused edit/check branches tied to `angdocs` findings. Store durable notes, generated patch plans, and PR records under `../patches/`, `../batches/`, and `../findings/`.
